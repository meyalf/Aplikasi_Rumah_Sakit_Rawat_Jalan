<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== EXISTING PERMISSIONS ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- ===== NOTIFICATION & WORKMANAGER PERMISSIONS ===== -->

    <!-- Network State untuk WorkManager -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification Permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibrate untuk notification -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ===== TELEPON & WHATSAPP PERMISSIONS (BARU!) ===== -->

    <!-- Permission untuk telepon (membuka dialer, tidak langsung telepon) -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Permission untuk membuka aplikasi lain (WhatsApp) -->
    <queries>
        <package android:name="com.whatsapp" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplikasi_Rumah_Sakit_Rawat_Jalan">

        <!-- ===== ACTIVITIES ===== -->

        <!-- LoginActivity - LAUNCHER (dibuka pertama kali) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- AdminActivity -->
        <activity
            android:name=".AdminActivity"
            android:exported="false" />

        <!-- VerifikasiPendaftaranActivity -->
        <activity
            android:name=".VerifikasiPendaftaranActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- ManageDokterActivity -->
        <activity
            android:name=".ManageDokterActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- ManagePoliActivity -->
        <activity
            android:name=".ManagePoliActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- ManageObatActivity -->
        <activity
            android:name=".ManageObatActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- ManageJadwalActivity -->
        <activity
            android:name=".ManageJadwalActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- ManageAkunActivity -->
        <activity
            android:name=".ManageAkunActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- LaporanActivity -->
        <activity
            android:name=".LaporanActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- ManagePasienActivity -->
        <activity
            android:name=".ManagePasienActivity"
            android:exported="false"
            android:parentActivityName=".AdminActivity" />

        <!-- ===== PROVIDERS ===== -->

        <!-- FileProvider untuk share file -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager Initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>